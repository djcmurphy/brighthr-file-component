import type { JSX } from "react";

type FileIcons = Record<string, JSX.Element>;

export const FileIcons = {
  pdf: "file-pdf",
  doc: "file-document",
  csv: "file-spreadsheet",
  mov: "file-video",
};
